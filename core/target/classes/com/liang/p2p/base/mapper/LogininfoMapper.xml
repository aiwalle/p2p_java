<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liang.p2p.base.mapper.LogininfoMapper" >
  <resultMap id="BaseResultMap" type="com.liang.p2p.base.domain.Logininfo" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="username" property="username" jdbcType="VARCHAR" />
    <result column="password" property="password" jdbcType="VARCHAR" />
    <result column="state" property="state" jdbcType="TINYINT" />
  </resultMap>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id" >
    insert into logininfo (username, password, state)
    values (#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, #{state,jdbcType=TINYINT})
  </insert>
  <update id="updateByPrimaryKey" >
    update logininfo
    set username = #{username,jdbcType=VARCHAR},
      password = #{password,jdbcType=VARCHAR},
      state = #{state,jdbcType=TINYINT}
    where id = #{id,jdbcType=BIGINT}
  </update>
  
  
  <sql id="base_column">
    id, username, password, state
  </sql>
  
  
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long" >
    select <include refid="base_column"/>
    from logininfo
    where id = #{id,jdbcType=BIGINT}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select <include refid="base_column"/>
    from logininfo
  </select>

  <select id="getCountByUsername" resultType="int">
    SELECT count(id) FROM logininfo WHERE username = #{username}
  </select>

  <select id="login" resultMap="BaseResultMap">
    SELECT <include refid="base_column"/>
    FROM logininfo
    WHERE username = #{username} AND password = #{password}
  </select>
</mapper>